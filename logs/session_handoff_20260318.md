# DroneP_VG 会话交接报告

日期：2026-03-18

## 1. 本次新增完成

- 已按 `progress_save_protocol.md` 完成日志、长期总结、阶段总结、交接文档的同步更新。
- 已统一当前有效指标口径，并在 `project_records/summaries/` 内固化。
- 已更新 `project_records/README.md`，补齐本次新增日志文件清单。
- 已完成“整图 best 基线 vs 滑窗推理（同阈值）”对照实验，并生成结果文件。

## 2. 当前阶段判断

- 当前阶段仍处于“可复现实验闭环已完成、等待下一轮实证改进”的状态。
- 滑窗策略在当前 100 张评估子集上已优于整图 best 基线：Acc@0.5=0.3760, Acc@0.75=0.2813, mAP@0.5=0.1517。
- 本次无新增实验结果，结论未发生反转。

## 3. 当前未完成项

1. 接入滑窗切片推理并与整图 best 基线做同口径对照。
2. 产出 3-5 组成功案例与 3-5 组失败案例可视化素材。
3. 在 GPU 路径下复跑关键实验并记录与 CPU 结果的一致性和耗时差异。

说明：第 1 项已完成，后续可从第 2 项开始。

## 4. 下次建议顺序

1. 先完成“整图 vs 滑窗”同指标对照并写入 `project_records/results/metrics/`。
2. 再整理误差案例（重点关注类别错配与小目标漏检）。
3. 最后补齐 GPU 运行基准（单图/批量耗时、吞吐与显存占用）并更新总结文档。

更新后建议顺序：

1. 先整理滑窗成功/失败案例并生成可汇报素材。
2. 统计滑窗配置（窗口大小、重叠率）变化对精度与耗时的影响。
3. 补齐 GPU 耗时基准并与整图推理形成完整对照表。

## 5. 可直接引用的话

"继续从 session_handoff_20260318.md 往下做，优先补齐滑窗推理对照实验，并把结果按现有评估口径写入 project_records/results。"

## 6. 2026-03-18 晚间补充

### 本次新增完成
- 已完成 Stage A 外接校准头训练（40 epoch 与 200 epoch）。
- 已完成训练过程可视化：终端 epoch 进度条 + 实时进度文件（TSV）。
- 已完成长训练评估并写入统一实验报告（Exp-08）。
- 已补充指标解释文档：`protocols/note.md`。

### 当前阶段判断
- 当前阶段“外接模块训练闭环”已完成，且 Acc 指标优于先前 40 epoch 版本。
- 在“仅重排现有候选框”的约束下，Acc@0.5 上限接近 0.413，短期内无法达到 0.6。

### 当前未完成项
1. 若要继续提升 Acc@0.5，需要扩充候选框来源（不是仅做重排/校准）。
2. 补做训练/验证拆分评估，给出泛化能力证据。
3. 增加按类别与目标尺度的误差分析图表。

### 下次建议顺序
1. 先生成更高召回候选池（更低阈值或更多滑窗配置）。
2. 在新候选池上重训外接校准头并复评三指标。
3. 追加一版 train/val 拆分结果到统一实验报告。

### 可直接引用的话
"继续从 session_handoff_20260318.md 往下做，先扩充候选框召回，再重训外接校准头；目标是突破当前 Acc@0.5 的 0.413 上限约束。"

## 7. 2026-03-18 深夜补充（最新可继续状态）

### 本次新增完成
- 已完成外接模块三组关键对照：
	- Exp-10：检测指标导向奖励 + RL 早停 + 受限搜索；
	- Exp-11：偏召回 reward 权重（acc05_weight=0.90）；
	- Exp-14：微调主模型接入 Exp-09 同口径链路。
- 已新增并验证主模型微调脚本：`scripts/finetune_groundingdino_feasibility.py`。
- 已完成“参数量小幅提升”微调（head_plus，trainable=479）并得到可用 checkpoint。

### 当前阶段判断
- 当前最强综合结果（本轮）：Exp-14
	- Acc@0.5=0.4113
	- Acc@0.75=0.3021
	- mAP@0.5=0.1685
- 结论：主模型微调 + 外接模块可稳定拉高 mAP，并小幅提升 Acc@0.5；但高 IoU 指标仍偏弱。

### 当前未完成项
1. 做严格 train/val/test 分离的主模型微调验证（避免同集训练评估偏乐观）。
2. 增加 IoU 0.75 导向的 box regression 优化（回拉 Acc@0.75）。
3. 增加可视化误差分析（成功/失败样例与类别混淆）。

### 下次建议顺序
1. 固定 Exp-14 配置，先做 3 折或固定划分的主模型微调复现。
2. 在外接模块中加入 IoU 0.75 权重与更细阈值网格，对齐高 IoU 目标。
3. 产出可汇报案例图与误差表，补齐答辩材料。

### 可直接引用的话
"继续从 Exp-14 往下做，先把主模型微调改成严格 train/val/test 评估，再针对 Acc@0.75 做定向优化。"